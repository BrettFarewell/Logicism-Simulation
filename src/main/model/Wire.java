package model;

// Represents a wire objects that will propagate any source signal given
public class Wire {
    
    // EFFECTS: creates a wire with output set to "off" and x and y positions set to given x and y values
    public Wire(int x, int y) {

    }

    // EFFECT: set output to "on" and return true
    public boolean output() {
        return false; // STUB
    }
}
